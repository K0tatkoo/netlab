# Nothing in this app is reached by reflection or by name — no serialization, no
# JNI, no dynamic class loading — so the defaults in
# proguard-android-optimize.txt plus Compose's bundled rules are sufficient.
