# Changelog

Every release of NetLab, newest first. Versions are MAJOR.MINOR.PATCH — MAJOR
when something people rely on changes or goes away, MINOR for something new,
PATCH for a fix — and each is a git tag (`vX.Y.Z`) on the commit that
shipped. `tools/release` in Claudes Projects writes these entries.

## 1.2.1 — 2026-09-23

Calculator sliders no longer change when a scroll passes over them.

- Stop sliders changing when a scroll passes over them
- Tell the public-repo guard the Password field label is not a secret
- Add the MIT license ahead of going public
- Add a changelog; versions start at v1.2.0
- Keep the Gradle settings Android Studio added

## 1.2.0 — 2026-09-19 · versionCode 3

As published on n3d-store.com/apps (the app shows it as "1.2"). Settings can
now check n3d-store.com for a new version and install it itself, so an update
no longer means going back to the website. English and Czech.

Versions start here; everything earlier is in `git log`.
